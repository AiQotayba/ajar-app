# Enable workspace support
workspaces=true
# Use legacy peer deps to avoid conflicts
legacy-peer-deps=true
# Enable strict peer deps
strict-peer-deps=false
# Auto install peers
auto-install-peers=true
