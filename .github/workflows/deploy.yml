name: Deploy to Two Servers

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          printf "Host *\n\tStrictHostKeyChecking no\n" > ~/.ssh/config

      ###############################
      # 🔥 السيرفر الأول
      ###############################
      - name: web server
        run: ssh -i ~/.ssh/id_ed25519 ajarsyria@82.29.178.80 "./deploy.sh"

      ###############################
      # 🔥 السيرفر الثاني
      ###############################
      - name: dashboard server
        run: ssh -i ~/.ssh/id_ed25519 ajarsyria-dashboard@82.29.178.81 "./deploy.sh"
